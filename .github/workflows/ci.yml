concurrency:
  cancel-in-progress: true
  group: ${{ github.actor }}
jobs:
  build:
    env:
      CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: cachix/install-nix-action@v13
    - run: nix-channel --add https://nixos.org/channels/nixpkgs-unstable nixpkgs
    - run: nix-channel --update
    - run: nix-env -i cachix
    - run: nix-env -i just
    - run: just build '${{ matrix.python }}."about-time-3.1.1"'
    - run: just build '${{ matrix.python }}."absl-py-0.13.0"'
    - run: just build '${{ matrix.python }}."aioboto3-8.0.3"'
    - run: just build '${{ matrix.python }}."aioboto3-9.2.0"'
    - run: just build '${{ matrix.python }}."aiobotocore-1.0.4"'
    - run: just build '${{ matrix.python }}."aiobotocore-1.3.3"'
    - run: just build '${{ matrix.python }}."aiobotocore-1.4.1"'
    - run: just build '${{ matrix.python }}."aiodataloader-0.2.0"'
    - run: just build '${{ matrix.python }}."aioextensions-21.7.2261349"'
    - run: just build '${{ matrix.python }}."aiofiles-0.7.0"'
    - run: just build '${{ matrix.python }}."aiogqlc-1.0.5"'
    - run: just build '${{ matrix.python }}."aiogqlc-2.2.0"'
    - run: just build '${{ matrix.python }}."aiohttp-3.7.4.post0"'
    - run: just build '${{ matrix.python }}."aioitertools-0.8.0"'
    - run: just build '${{ matrix.python }}."aiomultiprocess-0.9.0"'
    - run: just build '${{ matrix.python }}."alive-progress-2.0.0"'
    - run: just build '${{ matrix.python }}."amazon-dax-client-2.0.0"'
    - run: just build '${{ matrix.python }}."aniso8601-9.0.1"'
    - run: just build '${{ matrix.python }}."antlr4-python3-runtime-4.7.2"'
    - run: just build '${{ matrix.python }}."antlr4-python3-runtime-4.9.2"'
    - run: just build '${{ matrix.python }}."anyio-3.3.0"'
    - run: just build '${{ matrix.python }}."appium-python-client-1.2.0"'
    - run: just build '${{ matrix.python }}."ariadne-0.13.0"'
    - run: just build '${{ matrix.python }}."asgiref-3.4.1"'
    - run: just build '${{ matrix.python }}."async-generator-1.10"'
    - run: just build '${{ matrix.python }}."async-lru-1.0.2"'
    - run: just build '${{ matrix.python }}."async-timeout-3.0.1"'
    - run: just build '${{ matrix.python }}."atomicwrites-1.4.0"'
    - run: just build '${{ matrix.python }}."attrs-21.2.0"'
    - run: just build '${{ matrix.python }}."authlib-0.15.4"'
    - run: just build '${{ matrix.python }}."aws-okta-processor-1.7.0"'
    - run: just build '${{ matrix.python }}."aws-sam-translator-1.38.0"'
    - run: just build '${{ matrix.python }}."aws-xray-sdk-2.8.0"'
    - run: just build '${{ matrix.python }}."awscli-1.20.31"'
    - run: just build '${{ matrix.python }}."backports.csv-1.0.7"'
    - run: just build '${{ matrix.python }}."backports.zoneinfo-0.2.1"'
    - run: just build '${{ matrix.python }}."beautifulsoup4-4.9.3"'
    - run: just build '${{ matrix.python }}."binaryornot-0.4.4"'
    - run: just build '${{ matrix.python }}."boto-2.49.0"'
    - run: just build '${{ matrix.python }}."boto3-1.12.32"'
    - run: just build '${{ matrix.python }}."boto3-1.17.106"'
    - run: just build '${{ matrix.python }}."boto3-1.18.31"'
    - run: just build '${{ matrix.python }}."boto3-1.18.32"'
    - run: just build '${{ matrix.python }}."boto3-type-annotations-0.3.1"'
    - run: just build '${{ matrix.python }}."botocore-1.15.32"'
    - run: just build '${{ matrix.python }}."botocore-1.15.49"'
    - run: just build '${{ matrix.python }}."botocore-1.20.106"'
    - run: just build '${{ matrix.python }}."botocore-1.20.112"'
    - run: just build '${{ matrix.python }}."botocore-1.21.31"'
    - run: just build '${{ matrix.python }}."botocore-1.21.32"'
    - run: just build '${{ matrix.python }}."botocore-1.21.36"'
    - run: just build '${{ matrix.python }}."bs4-0.0.1"'
    - run: just build '${{ matrix.python }}."bugsnag-4.1.0"'
    - run: just build '${{ matrix.python }}."cached-property-1.5.2"'
    - run: just build '${{ matrix.python }}."cachetools-4.2.2"'
    - run: just build '${{ matrix.python }}."category-encoders-2.2.2"'
    - run: just build '${{ matrix.python }}."certifi-2021.5.30"'
    - run: just build '${{ matrix.python }}."cffi-1.14.6"'
    - run: just build '${{ matrix.python }}."cfn-flip-1.2.3"'
    - run: just build '${{ matrix.python }}."cfn-lint-0.53.0"'
    - run: just build '${{ matrix.python }}."chardet-4.0.0"'
    - run: just build '${{ matrix.python }}."charset-normalizer-2.0.4"'
    - run: just build '${{ matrix.python }}."click-8.0.1"'
    - run: just build '${{ matrix.python }}."cloudmersive-virus-api-client-3.0.5"'
    - run: just build '${{ matrix.python }}."colorama-0.4.3"'
    - run: just build '${{ matrix.python }}."colorama-0.4.4"'
    - run: just build '${{ matrix.python }}."colorclass-2.2.0"'
    - run: just build '${{ matrix.python }}."confuse-1.5.0"'
    - run: just build '${{ matrix.python }}."contextlib2-21.6.0"'
    - run: just build '${{ matrix.python }}."contextvars-2.4"'
    - run: just build '${{ matrix.python }}."coverage-5.5"'
    - run: just build '${{ matrix.python }}."cryptography-3.4.8"'
    - run: just build '${{ matrix.python }}."cycler-0.10.0"'
    - run: just build '${{ matrix.python }}."cython-0.29.24"'
    - run: just build '${{ matrix.python }}."dataclasses-0.6"'
    - run: just build '${{ matrix.python }}."dataclasses-0.8"'
    - run: just build '${{ matrix.python }}."dateutils-0.6.12"'
    - run: just build '${{ matrix.python }}."decorator-4.4.2"'
    - run: just build '${{ matrix.python }}."decorator-5.0.9"'
    - run: just build '${{ matrix.python }}."defusedxml-0.7.1"'
    - run: just build '${{ matrix.python }}."delighted-4.1.0"'
    - run: just build '${{ matrix.python }}."deprecated-1.2.12"'
    - run: just build '${{ matrix.python }}."dill-0.3.4"'
    - run: just build '${{ matrix.python }}."dnslib-0.9.16"'
    - run: just build '${{ matrix.python }}."dnspython-2.1.0"'
    - run: just build '${{ matrix.python }}."docker-5.0.0"'
    - run: just build '${{ matrix.python }}."docopt-0.6.2"'
    - run: just build '${{ matrix.python }}."docutils-0.15.2"'
    - run: just build '${{ matrix.python }}."docutils-0.17.1"'
    - run: just build '${{ matrix.python }}."duckduckpy-0.2"'
    - run: just build '${{ matrix.python }}."dulwich-0.20.25"'
    - run: just build '${{ matrix.python }}."dynaconf-3.1.5"'
    - run: just build '${{ matrix.python }}."ecdsa-0.17.0"'
    - run: just build '${{ matrix.python }}."execnet-1.9.0"'
    - run: just build '${{ matrix.python }}."exponent-server-sdk-2.0.0"'
    - run: just build '${{ matrix.python }}."fpdf-1.7.2"'
    - run: just build '${{ matrix.python }}."freezegun-1.1.0"'
    - run: just build '${{ matrix.python }}."frozendict-2.0.6"'
    - run: just build '${{ matrix.python }}."future-0.18.2"'
    - run: just build '${{ matrix.python }}."geoip2-4.2.0"'
    - run: just build '${{ matrix.python }}."gitdb-4.0.7"'
    - run: just build '${{ matrix.python }}."gitpython-3.1.18"'
    - run: just build '${{ matrix.python }}."gitpython-3.1.20"'
    - run: just build '${{ matrix.python }}."google-auth-1.35.0"'
    - run: just build '${{ matrix.python }}."google-auth-2.0.2"'
    - run: just build '${{ matrix.python }}."google-auth-oauthlib-0.4.6"'
    - run: just build '${{ matrix.python }}."google-pasta-0.2.0"'
    - run: just build '${{ matrix.python }}."gql-2.0.0"'
    - run: just build '${{ matrix.python }}."grapheme-0.6.0"'
    - run: just build '${{ matrix.python }}."graphql-core-2.3.2"'
    - run: just build '${{ matrix.python }}."graphql-core-3.1.6"'
    - run: just build '${{ matrix.python }}."grpcio-1.39.0"'
    - run: just build '${{ matrix.python }}."gunicorn-20.1.0"'
    - run: just build '${{ matrix.python }}."h11-0.12.0"'
    - run: just build '${{ matrix.python }}."httpcore-0.13.3"'
    - run: just build '${{ matrix.python }}."httpcore-0.13.6"'
    - run: just build '${{ matrix.python }}."httplib2-0.19.1"'
    - run: just build '${{ matrix.python }}."httptools-0.3.0"'
    - run: just build '${{ matrix.python }}."httpx-0.19.0"'
    - run: just build '${{ matrix.python }}."hyperlink-21.0.0"'
    - run: just build '${{ matrix.python }}."idna-3.2"'
    - run: just build '${{ matrix.python }}."idna-ssl-1.1.0"'
    - run: just build '${{ matrix.python }}."immutables-0.16"'
    - run: just build '${{ matrix.python }}."importlib-metadata-4.8.1"'
    - run: just build '${{ matrix.python }}."importlib-resources-3.3.1"'
    - run: just build '${{ matrix.python }}."importlib-resources-5.2.2"'
    - run: just build '${{ matrix.python }}."iniconfig-1.1.1"'
    - run: just build '${{ matrix.python }}."itsdangerous-2.0.1"'
    - run: just build '${{ matrix.python }}."jinja2-3.0.1"'
    - run: just build '${{ matrix.python }}."jmespath-0.10.0"'
    - run: just build '${{ matrix.python }}."joblib-1.0.1"'
    - run: just build '${{ matrix.python }}."jsondiff-1.3.0"'
    - run: just build '${{ matrix.python }}."jsonpatch-1.32"'
    - run: just build '${{ matrix.python }}."jsonpointer-2.1"'
    - run: just build '${{ matrix.python }}."jsonschema-3.2.0"'
    - run: just build '${{ matrix.python }}."junit-xml-1.9"'
    - run: just build '${{ matrix.python }}."jwcrypto-1.0"'
    - run: just build '${{ matrix.python }}."kiwisolver-1.3.1"'
    - run: just build '${{ matrix.python }}."kiwisolver-1.3.2"'
    - run: just build '${{ matrix.python }}."lark-parser-0.10.1"'
    - run: just build '${{ matrix.python }}."lark-parser-0.11.3"'
    - run: just build '${{ matrix.python }}."lightgbm-3.2.1"'
    - run: just build '${{ matrix.python }}."lizard-1.17.9"'
    - run: just build '${{ matrix.python }}."localstack-client-1.23"'
    - run: just build '${{ matrix.python }}."lxml-4.6.3"'
    - run: just build '${{ matrix.python }}."mailchimp-marketing-3.0.59"'
    - run: just build '${{ matrix.python }}."mandrill-really-maintained-1.2.4"'
    - run: just build '${{ matrix.python }}."markdown-3.3.4"'
    - run: just build '${{ matrix.python }}."markupsafe-2.0.1"'
    - run: just build '${{ matrix.python }}."matplotlib-3.3.4"'
    - run: just build '${{ matrix.python }}."matplotlib-3.4.3"'
    - run: just build '${{ matrix.python }}."maxminddb-2.0.3"'
    - run: just build '${{ matrix.python }}."metaloaders-20.9.2566091"'
    - run: just build '${{ matrix.python }}."mirakuru-2.4.1"'
    - run: just build '${{ matrix.python }}."mixpanel-4.9.0"'
    - run: just build '${{ matrix.python }}."mock-4.0.3"'
    - run: just build '${{ matrix.python }}."more-itertools-8.4.0"'
    - run: just build '${{ matrix.python }}."more-itertools-8.8.0"'
    - run: just build '${{ matrix.python }}."moto-2.2.6"'
    - run: just build '${{ matrix.python }}."multidict-5.1.0"'
    - run: just build '${{ matrix.python }}."multiprocess-0.70.12.2"'
    - run: just build '${{ matrix.python }}."mypy-extensions-0.4.3"'
    - run: just build '${{ matrix.python }}."nest-asyncio-1.5.1"'
    - run: just build '${{ matrix.python }}."networkx-2.5.1"'
    - run: just build '${{ matrix.python }}."networkx-2.6.2"'
    - run: just build '${{ matrix.python }}."newrelic-6.8.1.164"'
    - run: just build '${{ matrix.python }}."numpy-1.19.5"'
    - run: just build '${{ matrix.python }}."numpy-1.21.2"'
    - run: just build '${{ matrix.python }}."oauth2client-4.1.3"'
    - run: just build '${{ matrix.python }}."oauthlib-3.1.1"'
    - run: just build '${{ matrix.python }}."opencv-python-4.5.3.56"'
    - run: just build '${{ matrix.python }}."oyaml-1.0"'
    - run: just build '${{ matrix.python }}."packaging-21.0"'
    - run: just build '${{ matrix.python }}."pandas-0.25.3"'
    - run: just build '${{ matrix.python }}."pandas-1.1.5"'
    - run: just build '${{ matrix.python }}."pandas-1.3.2"'
    - run: just build '${{ matrix.python }}."pathos-0.2.8"'
    - run: just build '${{ matrix.python }}."pathspec-0.9.0"'
    - run: just build '${{ matrix.python }}."patsy-0.5.1"'
    - run: just build '${{ matrix.python }}."pillow-8.3.1"'
    - run: just build '${{ matrix.python }}."pillow-8.3.2"'
    - run: just build '${{ matrix.python }}."pip-21.2.4"'
    - run: just build '${{ matrix.python }}."pip-upgrader-1.4.15"'
    - run: just build '${{ matrix.python }}."pluggy-0.13.1"'
    - run: just build '${{ matrix.python }}."pluggy-1.0.0"'
    - run: just build '${{ matrix.python }}."port-for-0.6.1"'
    - run: just build '${{ matrix.python }}."pox-0.3.0"'
    - run: just build '${{ matrix.python }}."ppft-1.6.6.4"'
    - run: just build '${{ matrix.python }}."prettytable-2.2.0"'
    - run: just build '${{ matrix.python }}."promise-2.3"'
    - run: just build '${{ matrix.python }}."protobuf-3.17.3"'
    - run: just build '${{ matrix.python }}."protobuf3-to-dict-0.1.5"'
    - run: just build '${{ matrix.python }}."psutil-5.8.0"'
    - run: just build '${{ matrix.python }}."psycopg2-2.9.1"'
    - run: just build '${{ matrix.python }}."py-1.10.0"'
    - run: just build '${{ matrix.python }}."pyaes-1.6.1"'
    - run: just build '${{ matrix.python }}."pyasn1-0.4.8"'
    - run: just build '${{ matrix.python }}."pyasn1-modules-0.2.8"'
    - run: just build '${{ matrix.python }}."pycocotools-2.0.0"'
    - run: just build '${{ matrix.python }}."pycparser-2.20"'
    - run: just build '${{ matrix.python }}."pydeps-1.9.14"'
    - run: just build '${{ matrix.python }}."pydriller-2.0"'
    - run: just build '${{ matrix.python }}."pyexcelerate-0.10.0"'
    - run: just build '${{ matrix.python }}."pygit2-1.6.1"'
    - run: just build '${{ matrix.python }}."pyjwt-2.1.0"'
    - run: just build '${{ matrix.python }}."pykwalify-1.8.0"'
    - run: just build '${{ matrix.python }}."pynamodb-5.1.0"'
    - run: just build '${{ matrix.python }}."pyparsing-2.4.7"'
    - run: just build '${{ matrix.python }}."pypdf4-1.27.0"'
    - run: just build '${{ matrix.python }}."pyrfc3339-1.1"'
    - run: just build '${{ matrix.python }}."pyrsistent-0.18.0"'
    - run: just build '${{ matrix.python }}."pytest-6.2.4"'
    - run: just build '${{ matrix.python }}."pytest-asyncio-0.15.1"'
    - run: just build '${{ matrix.python }}."pytest-cov-2.12.1"'
    - run: just build '${{ matrix.python }}."pytest-forked-1.3.0"'
    - run: just build '${{ matrix.python }}."pytest-freezegun-0.4.2"'
    - run: just build '${{ matrix.python }}."pytest-postgresql-3.1.1"'
    - run: just build '${{ matrix.python }}."pytest-random-order-1.0.4"'
    - run: just build '${{ matrix.python }}."pytest-rerunfailures-10.1"'
    - run: just build '${{ matrix.python }}."pytest-test-groups-1.0.3"'
    - run: just build '${{ matrix.python }}."pytest-timeout-1.4.2"'
    - run: just build '${{ matrix.python }}."pytest-xdist-2.3.0"'
    - run: just build '${{ matrix.python }}."python-dateutil-2.8.2"'
    - run: just build '${{ matrix.python }}."python-dotenv-0.19.0"'
    - run: just build '${{ matrix.python }}."python-git-2018.2.1"'
    - run: just build '${{ matrix.python }}."python-gitlab-2.10.1"'
    - run: just build '${{ matrix.python }}."python-hcl2-3.0.1"'
    - run: just build '${{ matrix.python }}."python-jose-3.3.0"'
    - run: just build '${{ matrix.python }}."python-magic-0.4.24"'
    - run: just build '${{ matrix.python }}."python-multipart-0.0.5"'
    - run: just build '${{ matrix.python }}."python-whois-0.7.3"'
    - run: just build '${{ matrix.python }}."python3-openid-3.2.0"'
    - run: just build '${{ matrix.python }}."pytz-2021.1"'
    - run: just build '${{ matrix.python }}."pyyaml-5.4.1"'
    - run: just build '${{ matrix.python }}."ratelimiter-1.2.0.post0"'
    - run: just build '${{ matrix.python }}."redis-3.5.3"'
    - run: just build '${{ matrix.python }}."redis-py-cluster-2.1.3"'
    - run: just build '${{ matrix.python }}."requests-2.26.0"'
    - run: just build '${{ matrix.python }}."requests-oauthlib-1.3.0"'
    - run: just build '${{ matrix.python }}."requests-toolbelt-0.9.1"'
    - run: just build '${{ matrix.python }}."responses-0.13.4"'
    - run: just build '${{ matrix.python }}."retry-0.9.2"'
    - run: just build '${{ matrix.python }}."retrying-1.3.3"'
    - run: just build '${{ matrix.python }}."returns-0.16.0"'
    - run: just build '${{ matrix.python }}."rfc3986-1.5.0"'
    - run: just build '${{ matrix.python }}."rsa-4.7.2"'
    - run: just build '${{ matrix.python }}."ruamel.yaml-0.17.16"'
    - run: just build '${{ matrix.python }}."ruamel.yaml.clib-0.2.6"'
    - run: just build '${{ matrix.python }}."rx-1.6.1"'
    - run: just build '${{ matrix.python }}."s3transfer-0.3.7"'
    - run: just build '${{ matrix.python }}."s3transfer-0.4.2"'
    - run: just build '${{ matrix.python }}."s3transfer-0.5.0"'
    - run: just build '${{ matrix.python }}."sagemaker-2.56.0"'
    - run: just build '${{ matrix.python }}."scikit-learn-0.24.2"'
    - run: just build '${{ matrix.python }}."scipy-1.5.4"'
    - run: just build '${{ matrix.python }}."scipy-1.7.1"'
    - run: just build '${{ matrix.python }}."seaborn-0.11.2"'
    - run: just build '${{ matrix.python }}."selenium-3.141.0"'
    - run: just build '${{ matrix.python }}."semantic-version-2.8.5"'
    - run: just build '${{ matrix.python }}."semver-2.13.0"'
    - run: just build '${{ matrix.python }}."send2trash-1.8.0"'
    - run: just build '${{ matrix.python }}."setuptools-57.4.0"'
    - run: just build '${{ matrix.python }}."setuptools-rust-0.12.1"'
    - run: just build '${{ matrix.python }}."sgqlc-14.0"'
    - run: just build '${{ matrix.python }}."simplejson-3.17.5"'
    - run: just build '${{ matrix.python }}."six-1.16.0"'
    - run: just build '${{ matrix.python }}."smdebug-rulesconfig-1.0.1"'
    - run: just build '${{ matrix.python }}."smmap-4.0.0"'
    - run: just build '${{ matrix.python }}."sniffio-1.2.0"'
    - run: just build '${{ matrix.python }}."social-auth-core-4.1.0"'
    - run: just build '${{ matrix.python }}."soupsieve-2.2.1"'
    - run: just build '${{ matrix.python }}."starlette-0.14.2"'
    - run: just build '${{ matrix.python }}."starlette-0.16.0"'
    - run: just build '${{ matrix.python }}."statsmodels-0.12.2"'
    - run: just build '${{ matrix.python }}."stdlib-list-0.8.0"'
    - run: just build '${{ matrix.python }}."tabulate-0.8.9"'
    - run: just build '${{ matrix.python }}."tensorboard-2.6.0"'
    - run: just build '${{ matrix.python }}."tensorboard-data-server-0.6.1"'
    - run: just build '${{ matrix.python }}."tensorboard-plugin-wit-1.8.0"'
    - run: just build '${{ matrix.python }}."terminaltables-3.1.0"'
    - run: just build '${{ matrix.python }}."thop-0.0.31-2005241907"'
    - run: just build '${{ matrix.python }}."threadpoolctl-2.2.0"'
    - run: just build '${{ matrix.python }}."tlslite-ng-0.7.6"'
    - run: just build '${{ matrix.python }}."toml-0.10.2"'
    - run: just build '${{ matrix.python }}."torch-1.4.0"'
    - run: just build '${{ matrix.python }}."torch-1.7.0"'
    - run: just build '${{ matrix.python }}."torch-1.9.0"'
    - run: just build '${{ matrix.python }}."torchvision-0.8.1"'
    - run: just build '${{ matrix.python }}."tqdm-4.62.2"'
    - run: just build '${{ matrix.python }}."tracers-21.8.6876"'
    - run: just build '${{ matrix.python }}."tree-sitter-0.19.0"'
    - run: just build '${{ matrix.python }}."typing-extensions-3.10.0.1"'
    - run: just build '${{ matrix.python }}."typing-extensions-3.10.0.2"'
    - run: just build '${{ matrix.python }}."tzdata-2021.1"'
    - run: just build '${{ matrix.python }}."tzlocal-3.0"'
    - run: just build '${{ matrix.python }}."urllib3-1.25.11"'
    - run: just build '${{ matrix.python }}."urllib3-1.26.6"'
    - run: just build '${{ matrix.python }}."uvicorn-0.15.0"'
    - run: just build '${{ matrix.python }}."uvloop-0.16.0"'
    - run: just build '${{ matrix.python }}."watchgod-0.7"'
    - run: just build '${{ matrix.python }}."watchtower-1.0.6"'
    - run: just build '${{ matrix.python }}."wcwidth-0.2.5"'
    - run: just build '${{ matrix.python }}."webob-1.8.7"'
    - run: just build '${{ matrix.python }}."websocket-client-1.2.1"'
    - run: just build '${{ matrix.python }}."websockets-9.1"'
    - run: just build '${{ matrix.python }}."werkzeug-2.0.1"'
    - run: just build '${{ matrix.python }}."wheel-0.37.0"'
    - run: just build '${{ matrix.python }}."wrapt-1.12.1"'
    - run: just build '${{ matrix.python }}."xmltodict-0.12.0"'
    - run: just build '${{ matrix.python }}."yarl-1.6.3"'
    - run: just build '${{ matrix.python }}."zenpy-2.0.24"'
    - run: just build '${{ matrix.python }}."zipp-3.5.0"'
    strategy:
      matrix:
        python:
        - python36
        - python37
        - python38
        - python39
name: ci
'on':
- pull_request
- push
